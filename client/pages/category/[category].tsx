import { PureComponent, Fragment } from "react";

export default class extends PureComponent {
    render() {
        return (
            <Fragment>
                CATEGORY_NAME
            </Fragment>
        )
    }
}